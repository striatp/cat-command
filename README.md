A small recreation of the classic `cat` command on UNIX systems. I used Assembly x86-64 to recreate it. This project simply was a way to practice Assembly.

### Assembly
```sh
make assemble
```

### Link
```sh
make link
```
